<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Apps on Ben's homepage</title><link>/blog/tags/apps/</link><description>Recent content in Apps on Ben's homepage</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Sun, 03 May 2026 10:38:57 +0000</lastBuildDate><atom:link href="/blog/tags/apps/index.xml" rel="self" type="application/rss+xml"/><item><title>Baby Sleep Sound</title><link>/blog/p/baby-sleep-sound/</link><pubDate>Sun, 03 May 2026 10:38:57 +0000</pubDate><guid>/blog/p/baby-sleep-sound/</guid><description>&lt;img src="/blog/p/baby-sleep-sound/cover.png" alt="Featured image of post Baby Sleep Sound" /&gt;&lt;h1 id="white-noise-generator-app"&gt;White noise generator app
&lt;/h1&gt;&lt;p&gt;I have a newborn, which means my Youtube history is full of searches for white noise videos. And they work very well to put a baby to sleep.&lt;/p&gt;
&lt;p&gt;Adding some Gemini-powered vibe coding into the mix, allow me to present: &lt;a class="link" href="https://ankel.github.io/apps/sleep-sound/" target="_blank" rel="noopener"
 &gt;Baby sleep sound&lt;/a&gt; is a single page app white noise generator. Better yet, it&amp;rsquo;s actually a single HTML file and can be opened locally (still need internet since it uses some Tailwind CSS though).&lt;/p&gt;
&lt;h2 id="how-it-works"&gt;How it works
&lt;/h2&gt;&lt;p&gt;True white noise is an equally random mix of every frequency. Using Web Audio API, the page periodically fills up a buffer full of random bytes. Playing that buffer and we get ourselves the white noise!&lt;/p&gt;
&lt;p&gt;Assuming no distortion by the audio processor, the speaker, the room or anything else, this should generate a sound with physically equal loudness across all frequencies. However, the human ear &lt;a class="link" href="https://en.wikipedia.org/wiki/Equal-loudness_contour" target="_blank" rel="noopener"
 &gt;perceives loudness differently depending on the frequency of the sound&lt;/a&gt;. Long story short, &lt;strong&gt;true white noise&lt;/strong&gt; has a lot of treble and sounds very harsh to the human ears. To address this, we can taper down the upper frequencies (pink noise), or even further quiet them down (brown noise). You can &lt;a class="link" href="https://en.wikipedia.org/wiki/Brownian_noise#Explanation" target="_blank" rel="noopener"
 &gt;read more about them in Wikipedia&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;The implementation of these noises is very interesting. The pink noise is generated through &lt;a class="link" href="https://github.com/ankel/ankel.github.io/blob/26cd73e2bbac61df18334895a5e08aca5758adb4/sleep-sound/index.htm#L437-L445" target="_blank" rel="noopener"
 &gt;a series of seemingly-random coefficients&lt;/a&gt;. While brown noise also uses seemingly random numbers, there are significantly fewer coefficients.&lt;/p&gt;
&lt;p&gt;Below is Gemini&amp;rsquo;s explanation for pink noise (Notes: the original values can be found on this mailing list &lt;a class="link" href="https://www.musicdsp.org/en/latest/Filters/76-pink-noise-filter.html#pink-noise-filter" target="_blank" rel="noopener"
 &gt;https://www.musicdsp.org/en/latest/Filters/76-pink-noise-filter.html#pink-noise-filter&lt;/a&gt;)&lt;/p&gt;

 &lt;blockquote&gt;
 &lt;p&gt;The specific decimal values are filter coefficients derived from an algorithm known as Paul Kellett&amp;rsquo;s refined method. Paul Kellett, an audio engineer, mathematically calculated these exact numbers to create a highly CPU-efficient series of recursive filters (IIR filters). When the outputs of these specific filters (b0 through b6) are summed together, they closely approximate the 1/f power spectral density required for authentic pink noise.&lt;/p&gt;

 &lt;/blockquote&gt;
&lt;p&gt;Its explanation for brown noise is a lot more questionable:&lt;/p&gt;

 &lt;blockquote&gt;
 &lt;p&gt;Brown noise, on the other hand, requires a steeper -6 dB per octave roll-off (1/f2). This happens to be the exact natural slope produced by a simple first-order low-pass filter, also known as a leaky integrator.&lt;/p&gt;
&lt;p&gt;The logic behind the brown noise algorithm you noticed (lastOut = (lastOut + (0.02 * white)) / 1.02) is exactly that: a leaky integrator. It mathematically integrates the white noise by continuously adding the new random value to the previous one. This integration creates the deep brown noise frequencies. The division by 1.02 is the &amp;ldquo;leak&amp;rdquo;—it slowly pulls the value back toward zero to prevent the accumulated numbers from drifting to infinity and causing the audio to clip.&lt;/p&gt;

 &lt;/blockquote&gt;
&lt;p&gt;While I was unable to confirm it entirely, the approach looks to be consistent with &lt;a class="link" href="https://en.wikipedia.org/wiki/Leaky_integrator" target="_blank" rel="noopener"
 &gt;Wikipedia&amp;rsquo;s formula of a leaky integrator&lt;/a&gt;, and a leaky integrator is indeed used in generating brown noise. Another subjective observation: my ear cannot tell this brown noise from a small sample of brown noises found on the internet.&lt;/p&gt;
&lt;h2 id="adding-shushing-sound"&gt;Adding shushing sound
&lt;/h2&gt;&lt;p&gt;My next step was to add a shushing sound. Apparently many babies like this - mine included. However, this sound is significantly harder to generate. Gemini chose to start with a white noise sample (uniformly random frequency) then put it through a low-pass filter. The final result doesn&amp;rsquo;t sound like shushing at all!&lt;/p&gt;
&lt;p&gt;I recorded my own shushing sound then put it through a spectrum analyzer to see if I can arrive at a better solution. Long story short: I could not. My gut feeling is that the shushing sound requires a specific distribution of frequencies, and sampling randomly is not sufficient. However, I lack the right knowledge to pursue this further.&lt;/p&gt;
&lt;p&gt;I also stopped pursuing it is because I ended up preferring this result: It sounds like the ocean waves crashing!&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Side note&lt;/strong&gt;: When I said &amp;ldquo;a spectrum analyzer&amp;rdquo;, I mean an app I found on the store. It&amp;rsquo;s amazing how we walks around with a mini physic lab in our pocket!&lt;/p&gt;
&lt;h2 id="other-flairs"&gt;Other flairs
&lt;/h2&gt;&lt;p&gt;I added a night sky visualization, a timer, and some option to make playing this on mobile better. As usual, let me know what you think!&lt;/p&gt;</description></item><item><title>Simple Secret Manager - or Rust vs Go</title><link>/blog/p/simple-secret-manager-or-rust-vs-go/</link><pubDate>Tue, 21 Apr 2026 18:13:16 +0000</pubDate><guid>/blog/p/simple-secret-manager-or-rust-vs-go/</guid><description>&lt;h2 id="simple-secret-manager"&gt;Simple secret manager
&lt;/h2&gt;&lt;p&gt;Over the first two or three weeks of my parental leave, I decided to work on a toy project. Here it is: &lt;a class="link" href="https://github.com/ankel/simple-secret-manager" target="_blank" rel="noopener"
 &gt;https://github.com/ankel/simple-secret-manager&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;In short, the goal of this project is to create an encrypted tarball storing loose secret files. If you manage your home lab with a few dozen passwords, API keys, tokens, this could be of use to you!&lt;/p&gt;
&lt;h2 id="rust-vs-go"&gt;Rust vs Go
&lt;/h2&gt;&lt;p&gt;There are two implementations of SSM in that repo, one in Rust and one in Golang. I&amp;rsquo;ve been spending the last few years working with Go, so that part is perhaps obvious, and as for Rust, well I just wanted to learn a new language. So how was it?&lt;/p&gt;
&lt;p&gt;Let&amp;rsquo;s start with some observations:&lt;/p&gt;
&lt;h3 id="line-of-code-vs-binary-size"&gt;Line of code vs binary size
&lt;/h3&gt;&lt;p&gt;Number wise, both languages clock around two thousand line of code for the whole project. For both languages, I try to stick to the standard library whenever possible (impossible for rust, as you&amp;rsquo;ll see below), and otherwise sticking to the popular dependencies.&lt;/p&gt;
&lt;p&gt;On the other hand, the Rust binary is much smaller. The Go code compiled to 4-5 MB whereas the Rust binary was only around 2.5MB. This is with the Go binary already stripped of debug symbols (&lt;code&gt;-ldflags &amp;quot;-s -w&amp;quot;&lt;/code&gt;). If I have to make a guess, I&amp;rsquo;d say the extra size came from the garbage collector? Go&amp;rsquo;s limited and obscure flag system definitely doesn&amp;rsquo;t help with understanding what&amp;rsquo;s going on, as opposed to Rust&amp;rsquo;s well-documented &lt;a class="link" href="https://doc.rust-lang.org/cargo/reference/profiles.html" target="_blank" rel="noopener"
 &gt;release profiles&lt;/a&gt;&lt;/p&gt;
&lt;h3 id="standard-library"&gt;Standard library
&lt;/h3&gt;&lt;p&gt;The concept of a &amp;ldquo;standard&amp;rdquo; library is quite different between the two languages. Go takes a &amp;ldquo;batteries-included&amp;rdquo; approach, providing built-in packages for common needs like JSON serialization, hashing, encryption, gzip, and tar. Rust, conversely, ships with a very slim standard library. All those aforementioned features require pulling in third-party &amp;ldquo;crates&amp;rdquo; (Rust&amp;rsquo;s term for dependencies). The developer experience in Rust is that you spend more time hunting for the right crate and managing dependencies before you can actually start building, even for a security focus project where I try not to rely too much on non-standard stuff. I quickly realized this is not a very realistic goal with Rust 😃&lt;/p&gt;
&lt;p&gt;However, there is a flip side to this ecosystem difference. While Go&amp;rsquo;s standard library is comprehensive, its APIs are often quite low-level. It gives you the primitives, but you usually have to wire them together yourself. Take extracting a tarball as an example. In Go, I had to manually read the tar header, iterate through it file by file, programmatically recreate the directory structure and extract the file. In Rust, using the popular &lt;code&gt;tar&lt;/code&gt; crate, that entire operation is abstracted behind a single function call.&lt;/p&gt;
&lt;h3 id="error-handling"&gt;Error handling
&lt;/h3&gt;&lt;p&gt;I&amp;rsquo;ll say it: I much prefer Rust&amp;rsquo;s approach to error handling!&lt;/p&gt;
&lt;p&gt;In Go, errors tend to congeal around a &amp;ldquo;stringly&amp;rdquo; typed interface - an &lt;code&gt;Error()&lt;/code&gt; function returning a string. While you &lt;strong&gt;can&lt;/strong&gt; define and handle specific error types, few &lt;strong&gt;do&lt;/strong&gt;. Take Go&amp;rsquo;s internal &lt;code&gt;net&lt;/code&gt; package: it defines specific errors (like DNS vs. transport), yet functions still return the generic &lt;code&gt;error&lt;/code&gt; interface. Differentiating between &amp;ldquo;is the cable unplugged?&amp;rdquo; and &amp;ldquo;can&amp;rsquo;t resolve the domain&amp;rdquo; requires an ugly, inefficient chain of &lt;code&gt;if errors.Is(err, typeA) else if...&lt;/code&gt; checks.&lt;/p&gt;
&lt;p&gt;Then there is the ubiquitous &lt;code&gt;if err != nil { return err }&lt;/code&gt; pattern. It suffers from two major flaws. First, it is so verbose that your brain is trained to ignore it, creating a perfect hiding spot for bugs. You might accidentally write a complex condition like &lt;code&gt;if foo.bar().baz != 42 &amp;amp;&amp;amp; err == nil&lt;/code&gt;, or worse, forget to check the error entirely. Go&amp;rsquo;s compiler won&amp;rsquo;t stop you; it just becomes a runtime bug. Oh, and did you notice a bug in the line above?&lt;/p&gt;
&lt;p&gt;Second, simply returning &lt;code&gt;err&lt;/code&gt; up the call stack is sloppy. It strips away the context of where the error originated. In a microservice architecture, dealing with a vague network error in the log without the context of &lt;strong&gt;which&lt;/strong&gt; service failed is a nightmare.&lt;/p&gt;
&lt;p&gt;Rust solves this elegantly. You can define your own error types using Enums, each carrying specific context (like an HTTP code or IO timeout). The caller is forced by the compiler to handle them, and using Rust&amp;rsquo;s pattern-matching makes routing those specific errors highly readable:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-rust" data-lang="rust"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;match&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fetch_data&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;{&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nb"&gt;Ok&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;data&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&amp;gt;&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;process&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;data&lt;/span&gt;&lt;span class="p"&gt;),&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nb"&gt;Err&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;NetError&lt;/span&gt;::&lt;span class="n"&gt;DnsUnresolved&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;domain&lt;/span&gt;&lt;span class="p"&gt;))&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&amp;gt;&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="fm"&gt;println!&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="s"&gt;&amp;#34;Cannot resolve: &lt;/span&gt;&lt;span class="si"&gt;{}&lt;/span&gt;&lt;span class="s"&gt;&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;domain&lt;/span&gt;&lt;span class="p"&gt;),&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nb"&gt;Err&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;NetError&lt;/span&gt;::&lt;span class="n"&gt;Timeout&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;duration&lt;/span&gt;&lt;span class="p"&gt;))&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&amp;gt;&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="fm"&gt;println!&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="s"&gt;&amp;#34;Timed out after: &lt;/span&gt;&lt;span class="si"&gt;{:?}&lt;/span&gt;&lt;span class="s"&gt;&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;duration&lt;/span&gt;&lt;span class="p"&gt;),&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nb"&gt;Err&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;NetError&lt;/span&gt;::&lt;span class="n"&gt;Disconnected&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&amp;gt;&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="fm"&gt;println!&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="s"&gt;&amp;#34;Cable unplugged!&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;),&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;}&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;While it&amp;rsquo;s still possible to propagate the underlying errors ie. &lt;code&gt;io.Error&lt;/code&gt; in application code, it&amp;rsquo;s hard to skip over the other option when it is this good!&lt;/p&gt;
&lt;p&gt;And if you really really don&amp;rsquo;t care about your errors, there&amp;rsquo;s &lt;a class="link" href="https://crates.io/crates/anyhow" target="_blank" rel="noopener"
 &gt;anyhow&lt;/a&gt;.&lt;/p&gt;
&lt;h3 id="memory-security"&gt;Memory Security
&lt;/h3&gt;&lt;p&gt;When building a secret manager, how the language handles memory is a critical security vector. Rust’s deterministic memory management really shines here. Because Rust lacks a garbage collector, a heap-allocated variable only lives as long as it remains in scope. Once it goes out of scope, the variable is immediately &amp;ldquo;dropped&amp;rdquo; (Rust&amp;rsquo;s term for freed). You can rely on the compiler to handle this automatically, or you can implement a custom &lt;code&gt;Drop&lt;/code&gt; trait for specific teardown behavior.&lt;/p&gt;
&lt;p&gt;Oh, and this scope is strictly checked by the compiler - this is the infamous borrow checker! At compile time, the compiler can tell who is owning the variable, and whether it&amp;rsquo;s still in scope or not. It can deterministically tell that when the &lt;code&gt;drop&lt;/code&gt; function is called, no one else is using that variable. Here&amp;rsquo;s an example where this is valuable:&lt;/p&gt;
&lt;p&gt;In my SSM implementation, I used Rust&amp;rsquo;s version of the &lt;code&gt;age&lt;/code&gt; cryptography crate. It includes a specific &amp;ldquo;secret string&amp;rdquo; type designed for storing passwords. One aspect of this type that - using these strict rules - it can securely zeroes out the string in memory before freeing (&lt;code&gt;drop&lt;/code&gt;ing) it, ensuring the plaintext password doesn&amp;rsquo;t linger in RAM longer than it needs to. This is possible because Rust compiler forces every function to agree on who owns the data:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Consuming (Moving)&lt;/strong&gt;: If a Rust &lt;code&gt;decrypt(key: SecretString)&lt;/code&gt; function takes ownership of the password, its scope &amp;ldquo;moves&amp;rdquo; into that function. You, as the caller, can no longer use it. It will be securely dropped and zeroed the moment the decrypt function finishes.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Borrowing (Referencing)&lt;/strong&gt;: If the function signature is &lt;code&gt;decrypt(key: &amp;amp;SecretString)&lt;/code&gt;, you know for a fact it is only borrowing the password. The compiler guarantees that the decrypt function cannot mutate it, cannot drop it, and cannot hold onto that reference longer than the variable&amp;rsquo;s lifetime in your calling function. If &lt;code&gt;SecretString&lt;/code&gt; doesn&amp;rsquo;t implement &lt;code&gt;Clone&lt;/code&gt; then they may not even make a copy of the string internally - at least not easily 😊&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In Go, the situation is completely different. Because of the garbage collector, there is no guaranteed or deterministic lifetime for a variable. Even if the programmer may want to zero it out, they don&amp;rsquo;t know how many code are still referring to that string; there&amp;rsquo;s no &amp;ldquo;moving vs referencing&amp;rdquo; sematic like Rust above.&lt;/p&gt;
&lt;p&gt;Admittedly I don&amp;rsquo;t know enough about the Go GC, however with Java&amp;rsquo;s GC - specifically the G1 collector - there&amp;rsquo;s another problem: Live objects may get copied around several times during the compaction phase. This means even if you read all the code in your entire dependency tree, it&amp;rsquo;s &lt;strong&gt;still&lt;/strong&gt; not possible to reason about how many copies of the password are there in memory.&lt;/p&gt;
&lt;h2 id="final-thoughts"&gt;Final thoughts
&lt;/h2&gt;&lt;p&gt;After reimplementing the same project in two different language, my opinion is that I really don&amp;rsquo;t have an opinion 😳. I like what I&amp;rsquo;ve seen in Rust, and it made me want to try Rust out more - especially the elegant error handling and the strict guarantees around memory security.&lt;/p&gt;
&lt;p&gt;But if I have to pick one language to hit the market first, Go is clearly the right choice. The rapid development cycle provided by its comprehensive standard library and hand-off garbage collector is hard to beat for getting a working prototype out the door quickly.&lt;/p&gt;</description></item><item><title>Wealth Simulator</title><link>/blog/p/wealth-simulator/</link><pubDate>Sun, 18 Jan 2026 22:33:23 +0000</pubDate><guid>/blog/p/wealth-simulator/</guid><description>&lt;img src="/blog/p/wealth-simulator/cover.png" alt="Featured image of post Wealth Simulator" /&gt;&lt;p&gt;I spent a weekend (one afternoon and one morning, to be specific) playing with Gemini. The result: &lt;a class="link" href="https://ankel.github.io/apps/wealth-simulator/" target="_blank" rel="noopener"
 &gt;Wealth Simulator&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The goal of this app is to run 1,000 iteration of Monte Carlo simulation to see how the future may play out, based on some provided inputs. The inputs are rather straightforward but do let me know if anything can be improved.&lt;/p&gt;
&lt;h2 id="build-and-deploy"&gt;Build and Deploy
&lt;/h2&gt;&lt;p&gt;The app is written as a single React app, and deployed as a static site on Github Pages. The build and deployment is automated through &lt;a class="link" href="https://github.com/ankel/ankel.github.io/blob/cb48b711587796187101fd97522ea7aa38cf5eaa/.github/workflows/deploy-site-wealth-simulator.yaml" target="_blank" rel="noopener"
 &gt;Github Action&lt;/a&gt;.&lt;/p&gt;</description></item></channel></rss>